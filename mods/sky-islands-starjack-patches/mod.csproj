<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<OutputType>Library</OutputType>
		<TargetFramework>net480</TargetFramework>
		<PlatformTarget>x64</PlatformTarget>

		<RootNamespace>SkyIslandsStarjackPatches</RootNamespace>
		<AssemblyName>SkyIslandsStarjackPatches</AssemblyName>
		<OutputPath>1.6/Assemblies</OutputPath>
		<VersionPrefix>0.1.0.0</VersionPrefix>

		<!-- Do not generate pdb file -->
		<DebugType>none</DebugType>

		<LangVersion>latest</LangVersion>
		<DebugSymbols>false</DebugSymbols>
		<AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
		<EnableDefaultCompileItems>false</EnableDefaultCompileItems>
	</PropertyGroup>

	<ItemGroup>
		<!-- Mod -->
		<Compile Include="1.6/Source/*" />

		<!-- Rimworld — references the shared root-level RimworldForCICD submodule.
		     RimWorld compiles all DLC (Odyssey included) into Assembly-CSharp.dll,
		     so no separate Odyssey reference is needed. No Harmony: this mod uses
		     only def patches and vanilla worker subclasses. -->
		<Reference Include="../../RimworldForCICD/Managed/*.dll">
			<Private>False</Private>
		</Reference>
	</ItemGroup>
</Project>
